# Normalize line endings in the repo to LF; check out native on each platform.
* text=auto

# Shell scripts must stay LF even on Windows checkouts, or they break in bash/WSL.
*.sh text eol=lf

# Treat binary assets as binary (no line-ending munging, no diffs).
*.png binary
*.jpg binary
*.jpeg binary
*.wasm binary
